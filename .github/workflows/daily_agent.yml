name: TFM — Agente Diario de Mercado

on:
  schedule:
    # 17:30 ET = 21:30 UTC (horario verano) / 22:30 UTC (horario invierno)
    # Usamos 21:35 UTC para asegurar que el mercado US está cerrado
    - cron: '35 21 * * 1-5'
  workflow_dispatch:  # Permite ejecución manual desde GitHub

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v4

      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install \
            yfinance==0.2.40 \
            pandas==2.2.2 \
            numpy==1.26.4 \
            gspread==6.1.2 \
            google-auth==2.29.0

      - name: Ejecutar agente diario
        env:
          GMAIL_USER:                   ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD:           ${{ secrets.GMAIL_APP_PASSWORD }}
          GOOGLE_SERVICE_ACCOUNT_JSON:  ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          python agent_daily_update.py

      - name: Notificar si falla
        if: failure()
        run: |
          echo "❌ El agente falló. Revisa los logs en Actions."
